因为现在的工具有很多，所以我们可以通过工具路由 agent判断可能需要用到哪些工具，然后将可能用到的工具发送给执行任务的agent。然后就是可能每一个业务模块的执行逻辑不同，同时为了避免我想在执行一个具体任务的时候缺少某些步骤，我们可以将每个个模块的执行步骤封装成一个 skill。当模型需要执行这个任务的时候，就将这个 skill 加载到当前模型中。另外，不同的业务模块就可以使用不同的 skill 进行封装。Skill 中可以提供具体的步骤以及每一步应该使用哪些工具。Skill 规范化为统一格式（SKILL.md），明确分离出：元数据（Metadata）、执行 SOP（Instructions） 与 依赖工具（Dependencies） skills要渐进式披露加载 ，刚开始只给出一个包含所有skill的目录skill，需要用到哪个就用哪个 2. 思考过程显示化，让用户知道大模型在干什么 3. 目前是react模式，是否会用到多agent以及plan-excute范式

机制：系统把规范、SOP 和复杂流程写在项目的规则文件里（如 CLAUDE.md、skills/deploy.md）。

调度：System Prompt 中只有一句话：“当执行特定业务时，先使用 view_file 读取对应文档”。模型在需要时主动发起一次文件读取，加载该任务的 SOP。任务结束后，这些内容随着上下文滚动自然被裁剪。

ReAct 与 Plan-and-Execute **绝非互斥，而是处于不同认知与抽象层级的互补范式**。

在生产级复杂 Agent 架构中，两者结合的 **Plan-and-ReAct（宏观规划 + 微观试错）** 正是解决长链路、高动态业务的标准实践。

---

### 两大范式的本质差异与优缺点

| 维度 | ReAct 范式 (Reasoning + Acting) | Plan-and-Execute 范式 |
| --- | --- | --- |
| **认知层级** | **微观战术级**（边走边看、摸石头过河） | **宏观战略级**（谋定而后动、全局视盘） |
| **工作机制** | 单步“思考 $\to$ 执行 Tool $\to$ 观察环境反馈 $\to$ 再思考”的紧耦合贪心循环 | 先生成完整子任务列表（DAG/步骤清单），再依次调度执行器，最后动态评估 |
| **核心优势** | 对非确定性反馈容错极高；遇到接口报错或环境变化能自发纠错重试 | 全局视野强，避免局部最优；子任务可并行化；规划与执行解耦，大幅降低 Token 开销 |
| **致命短板** | **容易“近视”与迷航**：步数一旦超过 5~8 步，极易遗忘原目标、陷入死循环或幻觉 | **容易“刻舟求剑”**：如果环境外部状态与预测不一致（如中途接口报错），静态计划立即失效 |

* **为什么单用 ReAct 不行？**
ReAct 本质是贪心搜索（Greedy Search）。让大模型一步步探索处理“跨 3 个系统整理 5 类异常数据并生成报表”的任务时，模型很容易在第 6 步被某个字段的格式异常带离主线，耗尽 Token 却忘了最初要做什么。
* **为什么单用 Plan-and-Execute 也不行？**
现实系统充满意外（网络抖动、参数校验失败、权限边界）。如果计划里写了“第 2 步查询用户余额”，但接口返回了 `403 Forbidden`，缺乏局部探索机制的纯 Plan 体系将直接卡死崩溃。

---

### 最佳实践组合：分层 Plan-and-ReAct

成熟的工程做法是将两者结合为 **“Master-Worker”分层架构**：

```
[用户复杂需求]
      │
      ▼
┌──────────────┐
│ Planner 节点 │  (选用高智商推理模型，输出结构化 Task List)
└──────┬───────┘
       │  下发 Step 1: 提取并清洗日志
       ▼
┌──────────────┐
│ ReAct Worker │  (微观自主循环：调用 API、解析 JSON、报错自修复)
└──────┬───────┘
       │  返回局部结果或遇到阻断异常
       ▼
┌──────────────┐
│  Evaluator   │ ───(执行顺利)───► 继续分派 Step 2
│  / Re-Planner│ ───(严重意外)───► 触发全局 Re-Plan，重整剩余计划
└──────────────┘

```

1. **Planner（规划层）**：负责将宏观 Prompt 拆解为有依赖关系的确定性步骤（Sub-tasks）。规划阶段不调用具体业务工具，保证专注与低延迟。
2. **ReAct Executor（执行层）**：每个具体的 Sub-task 委派给一个轻量级 ReAct Worker。Worker 可以在其隔离的局部上下文中调用 2~3 个特定 Tool。遇到小错误（如入参格式有误），ReAct 自行在内部闭环纠错，不污染上层全局状态。
3. **Re-planner（动态纠偏层）**：当 Worker 彻底报错或环境产生破坏性变化时，向上抛出中断信号，触发 Planner 重新评估并更新剩余计划。

---

### 业界更先进的替代与进阶范式

除了这两个经典范式，现代 Agent 架构正在向更具鲁棒性、确定性与经济性的范式演进：

#### 1. 混合状态图编排（Workflow-as-Agent / Hybrid StateGraph）

* **现状**：让 LLM 完全自由动态规划（Plan）在企业级应用中依然存在不可控风险。
* **做法**：**宏观骨架走确定性有向图（Graph），微观节点交由 LLM 填充**。将核心流程（如审批流、状态转移、风控核验）用硬编码代码或图节点固定，仅在特定节点内部允许 Agent 依据 ReAct 自由处理多义输入。确定性与泛化能力兼备，是目前工业级（如 LangGraph 生产实践）最推荐的形态。

#### 2. 代码即动作（Code-as-Action / Programmatic Agent）

* **现状**：传统的 Function Calling（ReAct）依赖反复生成 JSON Schema，每次数组过滤、数学计算都要发起网络往返。
* **做法**：直接让大模型编写一段完整的 Python/JS 逻辑脚本，送入受限的轻量沙箱执行。
* **优势**：把原先需要 10 轮 ReAct 往返的数据分析任务，浓缩为一次性“写代码 $\to$ 沙箱运行 $\to$ 拿最终结果”。不仅将 Token 消耗降低 70% 以上，而且天然拥有编程语言原生的 `for/if/try-catch` 容错控制。

#### 3. 语言智能体树搜索与自我反思（LATS: Language Agent Tree Search & Reflexion）

* **现状**：无论是 ReAct 的线性贪心，还是单一维度的规划，一旦走入死胡同都难以系统性回滚。
* **做法**：引入强化学习与博弈树（MCTS）思想。
* 面对关键决策时，由模型并发采样多条行动路径（Rollout）。
* 引入独立的 Evaluator 模型给各分支的中间执行状态评分。
* 如果某条分支被证明走不通，触发回溯（Backtracking）并保留反思记忆（Reflexion），指导算法走向更高分的分支。适合复杂代码生成、定理证明与多约束物流排程等高价值场景。



---

### 架构选择决策推荐

* **单一实体交互、步骤在 3 步以内**：直接采用原生 **ReAct**（单模型 Function Calling），架构最轻。
* **跨系统长链路、目标明确但依赖多接口**：采用 **Plan-and-ReAct（分层 Planner + ReAct Worker）**。
* **涉及重度数据处理、多表聚合、公式计算**：采用 **Code-as-Action（沙箱代码生成与执行）**。
* **企业严肃业务（涉及资金、权限、合规交付）**：切忌完全依赖开放式自主规划，必须采用 **确定性状态图（StateGraph）统领全局，关键分支节点嵌入受限 ReAct**。